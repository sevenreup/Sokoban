<Window x:Class="Sokoban.views.LevelComplete"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sokoban.views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100"
        Height="100"
        Width="200"
        WindowStyle="None"
             Background="LightGreen" ResizeMode="NoResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner"
        >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Content="You have finished the game" HorizontalAlignment="Center" VerticalAlignment="Center" MouseDown="Label_MouseDown"/>

    </Grid>
</Window>
